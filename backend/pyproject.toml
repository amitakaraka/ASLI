[project]
name = "app"
version = "0.1.0"
dependencies = [ "flask", "flask-sqlalchemy", "flask-cors", "flask-socketio==5.5.1", "python-socketio==5.12.0", "eventlet==0.39.1", "pyjwt", "werkzeug", "gunicorn==21.2.0", "simple-websocket", "redis>=5.0.0", "psycopg[binary]>=3.1.0" ]
requires-python = "~=3.12.0"
